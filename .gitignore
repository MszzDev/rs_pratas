# Segredos — nunca commitar
.env
.env.*
!.env.example
# Só o endereço público da API — sem segredo, e sem ele o APK nasce quebrado.
!.env.production
!.env.*.example
*.pem
*.key
*.p12
*.jks
*.keystore
**/credentials/
**/secrets/

# Cópias do banco. NUNCA no Git: o repositório é público e o dump tem dados
# de clientes e hashes de senha.
backups/

# Dependências / build
node_modules/
dist/
build/
.turbo/
coverage/
*.tsbuildinfo

# Android / Capacitor
apps/web/android/local.properties
apps/web/android/app/*.keystore
apps/web/android/.gradle/
apps/web/android/app/build/
apps/web/android/build/
apps/web/ios/

# Documentos enviados pelos funcionarios — dado sensivel, nunca no repositorio
apps/api/storage/

# Prisma
apps/api/prisma/*.db

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
